# `src/components/`

Reusable view components shared by pages.

- `AppHeader.tsx` — top navigation/header shell.
- `BinaryInput.jsx` — binary bit entry + visual field overlay.
- `runtime/` — runtime-specific editor/toolbar/sidebar components.
