# `src/pages/runtime/hooks/`

Hooks scoped to the runtime page workflow (not app-wide reusable primitives).

- `useRuntimeDebugActions.ts` — build/init/step/run/reset orchestration for debug worker using runtime providers.
- `useRuntimeEditorState.ts` — Monaco mount/highlight/change behavior on top of provider state/selectors.
